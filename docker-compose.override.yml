version: "3.8"
services:
  localai:
    image: localai:inpainting
    build:
      context: .
      dockerfile: docker/Dockerfile.inpainting
    ports:
      - "3007:3007"
    environment:
      - PORT=3007
    restart: unless-stopped
