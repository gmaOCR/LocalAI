# Configuration pour le registry privé mercure.gregorymariani.com
# Copiez ce fichier vers .env.registry et remplissez vos credentials
# 
# Usage:
#   cp .env.registry.example .env.registry
#   # Éditez .env.registry avec vos credentials
#   source .env.registry
#   source scripts/registry-config.sh
#   ./scripts/build-and-push.sh

# Registry credentials (REQUIS pour pousser l'image)
export REGISTRY_USER="votre-username"
export REGISTRY_TOKEN="votre-token-ou-password"

# Configuration optionnelle (déjà définie dans registry-config.sh)
# export REGISTRY="mercure.gregorymariani.com"
# export IMAGE_NAME="localai"
# export IMAGE_TAG="inpainting-latest"
# export BUILD_TYPE=""  # vide pour CPU, "cublas" pour CUDA, "hipblas" pour AMD
